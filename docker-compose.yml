 services:
   app:
     build: .
     ports:
       - "80:80"
       - "443:443"
       - "443:443/udp"
     restart: always
     volumes:
       - caddy_data:/data
       - caddy_config:/config
     env_file:
       - .env.docker


 volumes:
   caddy_data:
   caddy_config:


